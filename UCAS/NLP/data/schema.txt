Node: Director(name string,numberOfMoviesActedIn int),Actor(name string,numberOfMoviesActedIn int),Person(name string,numberOfMoviesActedIn int),Movie(title string, released date, year int, imdbRating float);
Relationships: ACTED_IN[from Actor to Movie], DIRECTED[from Director to Movie];
Notes: Actor and Director may have intersection; Person is the union of Director and Actor.